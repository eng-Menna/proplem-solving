#include <bits/stdc++.h>
#define IOS ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);
using namespace std;
int32_t main()
{ IOS

string aString; cin>>aString; set<char>aSet;
    for (auto &i: aString) { aSet.insert(i); }
    if(aSet.size()&1)
        cout<<"IGNORE HIM!\n";
    else
        cout<<"CHAT WITH HER!\n";



}
